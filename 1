var a: array[1..20] of integer;
n,min: integer;
begin
 write('Количество n');
 readln(n);
 writeln('Элементы массива');
 for var i:=1 to n do
   read(a[1]);
 min:=a[1];
 for var i :=2 to n do
   if a[i] < min then
     min := a[i];
   writeln('Минимум=',min)
end.
